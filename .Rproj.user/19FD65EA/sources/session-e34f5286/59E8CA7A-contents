box::use(
  DBI[dbConnect],
  RPostgres[Postgres]
)

#' @export
conn <- DBI::dbConnect(
  RPostgres::Postgres(),
  dbname = "postgres",
  user = Sys.getenv("POSTGRES_USER"),
  host = "data.pr.uoguelph.ca",
  port = "5432",
  password = Sys.getenv("POSTGRES_PASS"),
)
